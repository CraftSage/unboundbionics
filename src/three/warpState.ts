// Drives the "follow the signal" warp tunnel. active = how visible/engaged the
// tunnel is (0..1), progress = how far through it the camera has flown (0..1).
export const warpState = {
  active: 0,
  progress: 0,
}
